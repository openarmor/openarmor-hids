[Unit]
Description=openarmor Execd
PartOf=openarmor-server.target

[Service]
EnvironmentFile=/etc/openarmor-init.conf
Environment=DIRECTORY=/var/openarmor

ExecStartPre=/usr/bin/env ${DIRECTORY}/bin/openarmor-execd -t
ExecStart=/usr/bin/env ${DIRECTORY}/bin/openarmor-execd -f
