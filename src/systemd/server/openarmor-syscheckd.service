[Unit]
Description=openarmor syscheckd
PartOf=openarmor-server.target

[Service]
EnvironmentFile=/etc/openarmor-init.conf
Environment=DIRECTORY=/var/openarmor

ExecStartPre=/usr/bin/env ${DIRECTORY}/bin/openarmor-syscheckd -t
ExecStart=/usr/bin/env ${DIRECTORY}/bin/openarmor-syscheckd -f
